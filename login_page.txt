import tkinter
# To configure once other bugs fixed

class loginPage(sizex, sizey, title):
  def __init__(self):
    self.root = tkinter.Tk()
    self.root.geometry(f"{sizex}x{sizey}")
    self.root.title(title)
    self.root.iconbitmap("icon.ico")
    self.root.mainloop()
